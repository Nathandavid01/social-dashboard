/**
 * App version shown in the UI and changelog.
 *
 * Convention (bump on every merge to main):
 *   - MAJOR (big number)  → a big feature / milestone
 *   - MINOR (small number) → a small feature or fix
 *
 * e.g. 1.4 → 1.5 for a small feature, 1.4 → 2.0 for a big one.
 */
export const APP_VERSION = '2.45'
